{
  "name": "breast-cancer-app",
  "version": "1.0.0",
  "description": "Full-Stack Breast Cancer Diagnostic Web Application (MERN + Python ML Bridge)",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"CLIENT,SERVER\" -c \"cyan,magenta\" \"npm run dev --prefix client\" \"npm run start --prefix server\"",
    "start": "npm run start --prefix server",
    "client": "npm run dev --prefix client",
    "server": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
